# Проект: E-shop MK (Учебный проект)

## 🚀 О проекте

Это мой первый комплексный учебный проект, созданный в рамках обучения Frontend-разработке. Цель проекта — создать макет одностраничного интернет-магазина, реализовать динамическую отрисовку каталога товаров и базовую интерактивность на "чистом" JavaScript (Vanilla JS).

Проект выполнен в стилистике "киберпанк/неон".

## 🛠️ Стек технологий

*   **HTML5:** Семантическая верстка.
*   **CSS3:**
    *   Flexbox и Grid Layout для построения адаптивной сетки.
    *   CSS-переменные для управления дизайн-системой (цвета, шрифты).
    *   Адаптивная верстка с использованием `@media`-запросов.
    *   Продвинутые эффекты (`:hover`, `transition`, `transform`, `box-shadow`).
*   **JavaScript (ES6+):**
    *   Полная манипуляция DOM (`querySelector`, `createElement`, `append`, `classList`).
    *   Обработка событий (`addEventListener`).
    *   Работа с массивами данных (методы `forEach`, `filter`).
    *   Динамическая генерация HTML из JS-массива (рендер каталога).
    *   Реализация интерактивных компонентов: модальные окна, фильтрация товаров.

---

## ✨ Ключевые фичи

*   **Динамический каталог:** Карточки товаров не "захардкожены" в HTML, а генерируются на лету из массива данных в JavaScript.
*   **Фильтрация товаров:** Реализована клиентская фильтрация товаров по категориям. При клике на фильтр интерфейс мгновенно перерисовывается, показывая только нужные товары.
*   **Адаптивный дизайн:** Макет корректно отображается на десктопных и мобильных устройствах.
*   **Интерактивные модальные окна:** Реализованы модальные окна для связи и просмотра ссылок, с логикой открытия/закрытия по кнопке, крестику и клику на оверлей.

---

## 📚 Чему я научился

В ходе работы над этим проектом я на практике освоил:
*   Принципы построения сложных, многокомпонентных макетов.
*   Разницу между статической и динамической версткой.
*   Как "оживлять" интерфейс с помощью JavaScript, реагируя на действия пользователя.
*   Важность разделения данных (массив в JS) и их представления (HTML-разметка).
*   Базовые и продвинутые техники отладки кода в браузере.